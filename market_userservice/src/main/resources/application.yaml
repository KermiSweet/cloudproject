# eureka配置
eureka:
  client:
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${eureka.instance.port}/eureka/
  instance:
    hostname: localhost
    port: 8082
server:
  port: 8083
spring:
  application:
    name: user-service
feign:
  hystrix:
    enabled: false